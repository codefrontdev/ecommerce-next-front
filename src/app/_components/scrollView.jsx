import React from 'react';
import styles from '../styles/styles.module.css'
const ScrollView = () => {
    return <div id={styles.scrollView} className='fixed top-0 w-full h-2 z-50'/>

}
export default ScrollView;
